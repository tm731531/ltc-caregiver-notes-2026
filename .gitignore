# 投影片照片、圖片（不上傳）
*.jpg
*.jpeg
*.png
slides/

# 錄音、逐字稿、轉錄程序、log（不上傳）
VOICE/
*.aac
*.m4a
*.txt
*.log

# 操作示範影片（體積大，不上傳；clips/ 只保留 .md 重點筆記）
*.mp4

# 原始講義 PDF（受版權、體積大，只留本機；整理後內容已進 notes.md）
*.pdf

# 已轉成 MD，不上傳 HTML
*.html

# 系統
.DS_Store
Thumbs.db

# 例外：考試網站(GitHub Pages)要用的 HTML 允許上傳
!docs/
!docs/*.html
